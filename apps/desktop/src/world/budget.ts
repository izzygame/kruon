export const WORLD_SCENE_BUDGET = Object.freeze({
  maxAgents: 2,
  maxMeshes: 32,
  maxGeometries: 32,
  maxMaterials: 32,
  maxTextures: 0,
  maxDevicePixelRatio: 1.25,
  visiblePollMs: 2_000,
  hiddenPollMs: 15_000,
});
