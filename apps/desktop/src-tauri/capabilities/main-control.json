{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-control",
  "description": "Trusted 2D control window with explicit local control commands.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "main-control-commands"
  ]
}
