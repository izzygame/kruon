"""kruon cli-adapters spike test suite (W1).

Pure-stdlib unittest. No network, no subprocess, no real model calls:
every test exercises the parser / manifest / schema against synthetic
fixtures or in-memory inputs. See ../README.md for the run commands.
"""
